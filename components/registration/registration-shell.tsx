import { cn } from "@/lib/utils";

const steps = [{ n: "01", label: "Register" }, { n: "02", label: "Upload CV" }, { n: "03", label: "Complete" }];

export function RegistrationShell({ step, children }: { step: 1 | 2 | 3; children: React.ReactNode }) {
  return <div className="mx-auto w-full max-w-7xl px-6 pb-24 pt-32"><div className="animate-enter-up mb-12 flex items-center gap-4"><span className="font-display text-[10px] uppercase tracking-[0.22em] text-accent">Registration / DETI+ 2026</span><div className="h-px flex-1 bg-border" /><span className="font-display text-[10px] uppercase tracking-[0.2em] text-muted-foreground">0{step} / 03</span></div><div className="grid gap-12 lg:grid-cols-[0.78fr_1.22fr] lg:gap-16"><aside className="animate-enter-up delay-100 lg:sticky lg:top-32 lg:self-start"><h1 className="max-w-[7ch] font-display text-6xl lowercase leading-[0.9] text-primary sm:text-7xl lg:text-8xl">secure your place at deti+</h1><div className="mt-8 space-y-3 text-sm text-muted-foreground"><p>September 29 – October 1, 2026</p><p>DETI, University of Aveiro</p></div><div className="mt-10 border-t border-border pt-8"><p className="mb-6 font-display text-xs uppercase tracking-[0.2em] text-muted-foreground">Your registration</p><div>{steps.map((item, index) => { const active = index + 1 === step; const completed = index + 1 < step; return <div key={item.n} className="grid min-h-16 grid-cols-[42px_22px_1fr]"><span className="pt-0.5 font-display text-[10px] tracking-widest text-muted-foreground">{item.n}</span><div className="relative flex justify-center"><div className={cn("relative z-10 mt-0.5 h-3 w-3 border-2 bg-background transition-[background-color,border-color,box-shadow] duration-500", active || completed ? "animate-accent-pulse border-accent bg-accent" : "border-muted-foreground/50")} />{index < steps.length - 1 ? <div className="absolute bottom-0 top-3 w-px bg-border" /> : null}</div><p className={cn("font-display text-sm uppercase tracking-[0.12em] transition-colors duration-300", active ? "text-primary" : "text-muted-foreground")}>{item.label}</p></div> })}</div></div><div className="mt-6 border border-border bg-card/40 p-5 transition-[border-color,transform] duration-300 hover:-translate-y-1 hover:border-accent/40"><p className="font-display text-xs uppercase tracking-[0.18em] text-accent">Your CV can wait</p><p className="mt-3 text-sm leading-relaxed text-muted-foreground">You can submit your CV after registration or later using the personal link we send by email.</p></div></aside><div className="animate-enter-right delay-200">{children}</div></div></div>;
}
