/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  basePath: '/deti-plus-26',
  images: {
    unoptimized: true,
  },
}

export default nextConfig
